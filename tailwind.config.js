const defaultTheme = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    fontFamily: {

      'display': ['Inconsolata'],
      'body': ['Inconsolata'],
    }

  },
  variants: {
    // Some useful comment
  },
  plugins: [
    // Some useful comment
  ],
};